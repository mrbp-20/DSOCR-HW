# ⚠️ Отчёт о попытке запуска обучения LoRA

**Исполнитель:** Николай (Cursor AI Senior ML Engineer)  
**Дата:** 09.01.2026, 23:52 MSK  
**Задание:** TASK_FOR_NIKOLAI_TRAINING_20260109.md (Issue #2)  
**Статус:** ⚠️ ПРОБЛЕМА ПРИ ЗАПУСКЕ

---

## 📋 Что было сделано

### 1. Подготовка к запуску

✅ **Проверены данные:**
- Train: 3 образца (`data/processed/train/images/`)
- Val: 1 образец (`data/processed/val/images/`)
- Metadata.json присутствуют

✅ **Проверена конфигурация:**
- `configs/training_config.yaml` настроен корректно
- Batch size: 1
- Epochs: 5
- Save/eval steps: 3

✅ **Исправлены зависимости:**
- `torch`: 2.5.1+cu121 (откат с 2.9.1 из-за конфликта с torchvision)
- `torchvision`: 0.20.1+cu121
- `peft`: 0.10.0
- `transformers`: 4.45.0

---

## 🚨 Проблема при запуске

### Описание проблемы

При запуске `train_lora.py` процесс начинается нормально, но **зависает/завершается при загрузке модели**.

**Симптомы:**
1. ✅ Процессор загружается успешно
2. ❌ Загрузка модели (`AutoModel.from_pretrained`) зависает или падает
3. ❌ Лог обрывается после сообщения о загрузке процессора
4. ❌ Процессы Python завершаются без явной ошибки в логе

**Лог-файлы:**
- `logs/train_lora_20260109_233426.log` - первая попытка
- `logs/train_lora_20260109_235213.log` - вторая попытка

**Последние строки лога:**
```
2026-01-09 23:52:14 - train_lora - INFO - Процессор загружен: deepseek-ai/DeepSeek-OCR (revision: 9f30c71f441d010e5429c532364a86705536c53a)
```

**Дальше процесс не идет.**

---

## 🔍 Анализ проблемы

### Возможные причины:

1. **Недостаточно памяти (VRAM)**
   - Модель DeepSeek-OCR большая (~14GB в float16)
   - RTX 5060 Ti имеет 16GB VRAM
   - Возможно, не хватает памяти для загрузки модели

2. **Ошибка при загрузке модели**
   - Ошибка может не попадать в лог (завершение процесса)
   - Проблема с `AutoModel.from_pretrained()` и `revision` параметром
   - Проблема с `device_map="auto"`

3. **Зависимости**
   - Возможна несовместимость после отката `torch` до 2.5.1
   - `peft 0.10.0` может иметь проблемы с `torch 2.5.1`

4. **Проблемы с HuggingFace cache**
   - Модель может загружаться из кэша неправильно
   - Проблема с `cache_dir` параметром

---

## 📊 Что проверено

### GPU статус:
- ✅ GPU доступен (RTX 5060 Ti 16GB)
- ✅ CUDA работает
- ⚠️ При зависании GPU не используется (процесс не доходит до GPU)

### Процессы:
- ✅ Python процессы запускаются
- ❌ Процессы завершаются при загрузке модели
- ❌ Нет явных ошибок в stdout/stderr

### Логи:
- ✅ Лог-файлы создаются
- ✅ Процессор загружается
- ❌ Лог обрывается после загрузки процессора

---

## 💡 Предложения для решения

### Решение #1: Уменьшить использование памяти

Попробовать загрузку модели с дополнительными параметрами:

```python
# В utils/trainer.py, метод load_model_and_processor()
self.model = AutoModel.from_pretrained(
    base_model,
    revision=revision,
    torch_dtype=torch_dtype,
    attn_implementation=model_config.get('attn_implementation', 'eager'),
    device_map="cpu",  # Сначала на CPU, потом переместим
    trust_remote_code=model_config.get('trust_remote_code', True),
    cache_dir=model_config.get('cache_dir'),
    low_cpu_mem_usage=True,  # Дополнительная оптимизация
    max_memory={0: "14GiB"}  # Ограничение памяти GPU
)
```

### Решение #2: Добавить обработку ошибок

Добавить более детальное логирование и обработку ошибок:

```python
try:
    self.logger.info("Начало загрузки модели...")
    self.model = AutoModel.from_pretrained(...)
    self.logger.info("Модель загружена успешно")
except Exception as e:
    self.logger.error(f"ОШИБКА ЗАГРУЗКИ МОДЕЛИ: {e}", exc_info=True)
    raise
```

### Решение #3: Проверить загрузку модели отдельно

Запустить тест загрузки модели (как в `test_model_access.py`) - он работает, значит проблема в `train_lora.py`.

### Решение #4: Использовать 8-bit quantization

Если проблема в памяти, можно попробовать 8-bit quantization (но это может сломать LoRA).

---

## 📝 Рекомендации для Сёмы

Сёма, при запуске обучения возникла проблема:

**Ситуация:**
- Процесс начинается нормально
- Процессор загружается
- Загрузка модели зависает/падает

**Что нужно:**
1. Проверить, почему `AutoModel.from_pretrained()` зависает в `train_lora.py`, хотя в `test_model_access.py` работает
2. Возможно, проблема с `device_map="auto"` или другими параметрами
3. Может потребоваться дополнительная диагностика (логирование, try-except)

**Что работает:**
- ✅ Данные подготовлены
- ✅ Конфигурация правильная
- ✅ Зависимости совместимы
- ✅ Процессор загружается
- ✅ `test_model_access.py` работает (модель загружается)

**Следующие шаги:**
1. Сравнить код загрузки модели в `test_model_access.py` и `utils/trainer.py`
2. Добавить более детальное логирование
3. Попробовать загрузку модели с другими параметрами

---

## ✅ Что сделано правильно

1. ✅ Все зависимости установлены и совместимы
2. ✅ Конфигурация оптимизирована для 4 образцов
3. ✅ Данные подготовлены корректно
4. ✅ Процесс запускается
5. ✅ Процессор загружается

---

## ❌ Что не работает

1. ❌ Загрузка модели в `train_lora.py` зависает/падает
2. ❌ Нет детальных ошибок в логе
3. ❌ Процесс завершается без явной ошибки

---

## 🔄 Статус

**Текущий статус:** ⚠️ ТРЕБУЕТСЯ ДИАГНОСТИКА

**Блокер:** Загрузка модели в `train_lora.py`

**Готовность к обучению:** ❌ НЕТ (модель не загружается)

---

## 💬 Комментарии

Проблема интересная - `test_model_access.py` работает, а `train_lora.py` нет. Нужно сравнить код загрузки и понять, в чём разница.

Возможно, проблема в том, что в `train_lora.py` используется `LoRATrainer`, а загрузка модели происходит там, а не напрямую. Или проблема в параметрах конфига.

**Рекомендация:** Нужна помощь Сёмы для диагностики или дополнительное логирование для понимания, где именно зависает процесс.

---

**Статус:** ⚠️ ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ

**Подпись:** Николай (Senior ML Engineer, который нашёл проблему, но нужна помощь команды для решения) 🔍

**Дата:** 09.01.2026, 23:52 MSK
