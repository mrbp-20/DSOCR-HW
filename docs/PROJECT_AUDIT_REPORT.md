# Project Audit Report (06.01.2026)

**Автор:** Николай (Senior ML Engineer)  
**Дата:** 2026-01-06  
**Проект:** DSOCR-HW

---

## Понимание проекта

### Цель
Fine-tuning DeepSeek-OCR для распознавания рукописного текста с использованием LoRA-адаптеров. Проект решает задачу распознавания личных рукописных документов (заметки, архивы, формы), с которой плохо справляются классические OCR-системы.

### Команда
- **Владимир** (Product Owner) — определяет задачи, контролирует качество датасета, финальный ревью
- **Семён "Сёма"** (Tech Lead) — архитектура проекта, координация, code review, документация
- **Николай** (Senior ML Engineer, Cursor AI) — имплементация скриптов, fine-tuning, эксперименты

### Hardware
- **GPU**: NVIDIA GeForce RTX 5060 Ti 16 ГБ (CUDA Capability sm_120)
- **CUDA Driver**: 12.9 (576.88)
- **CUDA Toolkit**: 12.8.61
- **OS**: Windows 10
- **Python**: 3.10.10 (в venv)

---

## Текущее состояние

### Существующие файлы

**Корневая директория:**
- ✅ `README.md` — полное описание проекта, цели, команда, структура
- ✅ `.cursorrules` — правила работы для Cursor AI (Николай)
- ✅ `requirements.txt` — зависимости (⚠️ **есть опечатка**)
- ✅ `.gitignore` — правильно настроен для исключения данных, моделей, логов

**Конфигурация (`configs/`):**
- ✅ `training_config.yaml` — полная конфигурация для fine-tuning (LoRA, training params, data paths)
- ✅ `inference_config.yaml` — конфигурация для инференса

**Утилиты (`utils/`):**
- ✅ `logger.py` — полнофункциональный логгер с поддержкой timestamped файлов
- ✅ `image_processor.py` — класс `ImageProcessor` с методами предобработки (resize, enhance, denoise, normalize)
- ✅ `__init__.py` — инициализация модуля

**Скрипты (`scripts/`):**
- ⚠️ `__init__.py` — только заглушка, скрипты не реализованы

**Данные (`data/`):**
- ✅ `data/raw/` — директория существует (пустая)
- ✅ `data/annotated/` — директория существует (пустая)

### Существующие папки
- ✅ `configs/` — конфигурационные файлы
- ✅ `scripts/` — директория для скриптов
- ✅ `utils/` — утилиты
- ✅ `data/raw/` — сырые данные
- ✅ `data/annotated/` — размеченные данные
- ✅ `venv/` — виртуальное окружение

### Недостающие папки (согласно README.md)
- ❌ `data/processed/` — обработанные данные для тренировки (train/val)
- ❌ `models/` — модели и адаптеры
  - ❌ `models/base/` — кэш HuggingFace моделей
  - ❌ `models/lora_adapters/` — натренированные LoRA
- ❌ `logs/` — логи тренировок и ошибок
- ❌ `notebooks/` — Jupyter для экспериментов

---

## Окружение

### venv
- ✅ **Активирован** (судя по установленным пакетам в `venv/Lib/site-packages/`)

### Python
- ✅ **Версия**: 3.10.10 (соответствует требованиям: 3.10+)
- ✅ **Расположение**: `C:\DSOCR-HW\venv\Scripts\python.exe`

### Установленные пакеты

**Основные ML библиотеки:**
- ⚠️ `torch`: 2.7.1+cpu — **ПРОБЛЕМА**: установлена CPU-версия, нужна `2.9.1+cu128`
- ⚠️ `torchvision`: 0.22.1 — установлена, но не соответствует требованиям (нужна 0.21.0+)
- ❌ `torchaudio` — не установлен (требуется 2.9.1+)
- ❌ `transformers` — не установлен (требуется 4.55.0+)
- ❌ `peft` — не установлен (требуется 0.7.0+)
- ❌ `accelerate` — не установлен (⚠️ **есть опечатка в requirements.txt**)
- ❌ `bitsandbytes` — не установлен (требуется 0.41.0+)
- ❌ `datasets` — не установлен (требуется 2.16.0+)

**Обработка изображений:**
- ✅ `Pillow`: 11.2.1 (требуется 10.0.0+) — установлен
- ❌ `opencv-python` — не установлен (требуется 4.8.0+)

**Работа с данными:**
- ✅ `pandas`: 2.3.0 (требуется 2.0.0+) — установлен
- ✅ `numpy`: 2.0.2 (требуется 1.24.0+) — установлен
- ❌ `PyYAML` — не установлен (требуется 6.0.0+)

**Метрики и оценка:**
- ❌ `jiwer` — не установлен (требуется 3.0.0+)
- ❌ `scikit-learn` — не установлен (требуется 1.3.0+)

**Логирование и мониторинг:**
- ❌ `tensorboard` — не установлен (требуется 2.15.0+)
- ✅ `tqdm`: 4.67.0 (требуется 4.65.0+) — установлен

**Утилиты:**
- ❌ `huggingface-hub` — не установлен (требуется 0.20.0+)
- ❌ `safetensors` — не установлен (требуется 0.4.0+)
- ❌ `sentencepiece` — не установлен (требуется 0.1.99+)

**Вывод:** Большинство зависимостей из `requirements.txt` **не установлены**. Установлены только базовые пакеты (torch CPU, pandas, numpy, pillow, tqdm) и некоторые другие (spacy, flask и т.д.), которые не указаны в requirements.txt.

### Проблемы в requirements.txt

1. **⚠️ КРИТИЧЕСКАЯ ОПЕЧАТКА (строка 7):**
   ```
   acceleratе>=0.25.0  # ❌ кириллическая "е" вместо латинской "e"
   ```
   Должно быть: `accelerate>=0.25.0`

2. **⚠️ Несоответствие версий PyTorch:**
   - В requirements.txt: `torch>=2.9.1`
   - Установлено: `torch==2.7.1+cpu`
   - Нужна версия с CUDA: `torch>=2.9.1+cu128`

---

## Hardware

### GPU
- ✅ **Модель**: NVIDIA GeForce RTX 5060 Ti
- ✅ **Память**: 16311 MiB (≈16 ГБ) — соответствует требованиям
- ✅ **Использование**: 1906 MiB / 16311 MiB (≈11.7%)
- ✅ **CUDA Driver**: 12.9 (576.88)
- ✅ **Compute Capability**: sm_120 (как указано в требованиях)

### CUDA Driver
- ✅ **Версия**: 12.9 (576.88) — соответствует требованиям

### CUDA Toolkit
- ✅ **Версия**: 12.8.61 — соответствует требованиям (12.8)

### PyTorch видит GPU
- ❌ **НЕТ** — PyTorch установлен в CPU-версии (`2.7.1+cpu`)
- ⚠️ **Причина**: установлена CPU-версия PyTorch вместо CUDA-версии
- ⚠️ **Решение**: переустановить PyTorch с поддержкой CUDA 12.8

---

## Готовность к работе

### Что готово ✅

1. **Структура проекта:**
   - README.md с полным описанием
   - .cursorrules с правилами работы
   - .gitignore правильно настроен

2. **Конфигурация:**
   - `training_config.yaml` — полная конфигурация для fine-tuning
   - `inference_config.yaml` — конфигурация для инференса
   - Оба конфига учитывают специфику RTX 5060 Ti (sm_120, `attn_implementation: "eager"`)

3. **Утилиты:**
   - `utils/logger.py` — готов к использованию
   - `utils/image_processor.py` — готов к использованию

4. **Hardware:**
   - GPU доступен и работает
   - CUDA Driver и Toolkit установлены

5. **Виртуальное окружение:**
   - venv создан и активирован
   - Python 3.10.10 соответствует требованиям

### Что нужно сделать 🔧

#### Приоритет 1: Критические проблемы (блокируют работу)

1. **Исправить опечатку в requirements.txt:**
   - Строка 7: `acceleratе` → `accelerate`

2. **Переустановить PyTorch с CUDA:**
   ```powershell
   pip uninstall torch torchvision torchaudio
   pip install torch>=2.9.1 torchvision>=0.21.0 torchaudio>=2.9.1 --index-url https://download.pytorch.org/whl/cu128
   ```

3. **Установить все зависимости:**
   ```powershell
   pip install -r requirements.txt
   ```
   (после исправления опечатки)

4. **Проверить видимость GPU:**
   ```powershell
   python -c "import torch; print(torch.cuda.is_available())"
   ```
   Должно вернуть `True`

#### Приоритет 2: Создание недостающих директорий

1. **Создать структуру папок:**
   ```powershell
   mkdir -p data/processed/train
   mkdir -p data/processed/val
   mkdir -p models/base
   mkdir -p models/lora_adapters/handwriting_v1
   mkdir -p logs
   mkdir -p notebooks
   ```

2. **Создать .gitkeep файлы (если нужно):**
   - `models/base/.gitkeep`
   - `data/processed/.gitkeep`

#### Приоритет 3: Реализация скриптов (Issues)

1. **Issue #1: `scripts/prepare_dataset.py`**
   - Загрузка изображений из `data/raw/`
   - Чтение разметки из `data/annotated/annotations.csv`
   - Предобработка (resize, normalize, augmentation)
   - Split на train/val (80/20)
   - Сохранение в `data/processed/`

2. **Issue #2: `scripts/train_lora.py`**
   - Загрузка базовой модели DeepSeek-OCR
   - Настройка LoRA (rank=8, alpha=16)
   - Training loop с TensorBoard
   - Сохранение чекпоинтов каждые 500 steps
   - Логирование метрик (loss, CER)

3. **Issue #3: `scripts/evaluate.py`**
   - Загрузка модели + LoRA адаптера
   - Прогон на val-датасете
   - Расчёт CER, WER
   - Визуализация результатов
   - Сохранение отчёта

4. **Issue #4: `scripts/inference.py`**
   - CLI для распознавания одного изображения
   - Batch-обработка папки
   - Экспорт в TXT/JSON
   - Визуализация результатов

#### Приоритет 4: Тестирование

1. **Проверить работу утилит:**
   - `utils/logger.py` — создать тестовый лог
   - `utils/image_processor.py` — обработать тестовое изображение

2. **Проверить загрузку конфигов:**
   - Загрузить `training_config.yaml` и `inference_config.yaml`
   - Проверить корректность путей и параметров

---

## Вопросы к команде

### Технические вопросы

1. **Датасет:**
   - Когда ожидается первый датасет в `data/raw/`?
   - Какой формат будет у `data/annotated/annotations.csv`? (нужна структура для `prepare_dataset.py`)

2. **Модель:**
   - Нужно ли предварительно скачать базовую модель DeepSeek-OCR в `models/base/`?
   - Или она будет загружаться автоматически через HuggingFace Hub?

3. **LoRA параметры:**
   - В `training_config.yaml` указаны `r=8, alpha=16`. Это финальные значения или можно экспериментировать?
   - Нужны ли разные версии адаптеров (handwriting_v1, handwriting_v2, ...)?

### Рекомендации

1. **Исправить requirements.txt немедленно:**
   - Опечатка в `acceleratе` блокирует установку пакета

2. **Установить PyTorch с CUDA:**
   - Без CUDA-версии PyTorch невозможно использовать GPU для тренировки

3. **Создать минимальный тестовый датасет:**
   - 2-3 изображения рукописей для проверки пайплайна
   - CSV с разметкой для тестирования `prepare_dataset.py`

4. **Документировать формат annotations.csv:**
   - Структура колонок (image_path, text, ...)
   - Примеры записей

---

## Итоговая оценка готовности

**Общая готовность: ~30%**

- ✅ **Архитектура и документация**: 90% (README, конфиги, утилиты готовы)
- ❌ **Окружение**: 20% (зависимости не установлены, PyTorch CPU)
- ❌ **Скрипты**: 0% (не реализованы)
- ✅ **Hardware**: 100% (GPU доступен, CUDA установлен)

**Следующие шаги:**
1. Исправить requirements.txt
2. Установить зависимости (особенно PyTorch с CUDA)
3. Создать недостающие директории
4. Начать реализацию скриптов (Issue #1 → #4)

---

**Отчёт подготовлен:** Николай (Cursor AI)  
**Дата:** 2026-01-06

