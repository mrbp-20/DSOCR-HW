# 🔧 Инструкция: Фикс загрузки модели DeepSeek-OCR

**Дата:** 09.01.2026, 00:39 MSK  
**Исполнитель:** Николай (Cursor AI Senior ML Engineer)  
**Tech Lead:** Семён  
**Проблема:** ImportError для LlamaFlashAttention2

---

## 🎯 Задача

Решить проблему загрузки модели DeepSeek-OCR, используя локальный кэш вместо HuggingFace API.

---

## 📋 Вариант 1: Загрузка из локального кэша (ПОПРОБОВАТЬ ПЕРВЫМ)

### Шаг 1: Обновить `test_model_access.py`

Замени строку загрузки модели на:

```python
model = AutoModelForVision2Seq.from_pretrained(
    r"C:\Users\Vovan\.cache\huggingface\hub\models--deepseek-ai--DeepSeek-OCR\snapshots\9f30c71f441d010e5429c532364a86705536c53a",
    torch_dtype=torch.float16,
    attn_implementation="eager",  # КРИТИЧНО для RTX 5060 Ti (sm_120)
    device_map="auto",
    trust_remote_code=True,
    local_files_only=True  # ← НЕ качать с HuggingFace, использовать только локальные файлы
)
```

**Ключевые изменения:**
- Используем **полный путь** к локальному snapshot-у модели
- Добавлен `local_files_only=True` — модель будет загружена **только** из локального кэша
- Префикс `r` перед строкой пути (raw string) — чтобы не экранировать обратные слеши

### Шаг 2: Обновить `test_inference.py`

Аналогично замени загрузку модели:

```python
model = AutoModelForVision2Seq.from_pretrained(
    r"C:\Users\Vovan\.cache\huggingface\hub\models--deepseek-ai--DeepSeek-OCR\snapshots\9f30c71f441d010e5429c532364a86705536c53a",
    torch_dtype=torch.float16,
    attn_implementation="eager",
    device_map="auto",
    trust_remote_code=True,
    local_files_only=True
)
```

### Шаг 3: Запустить тесты

```powershell
cd C:\DSOCR-HW
.\venv\Scripts\Activate.ps1

# Тест 1: Проверка загрузки модели
python test_model_access.py

# Если Тест 1 прошёл успешно, запускаем Тест 2
python test_inference.py
```

### Ожидаемый результат

**Если сработает:**
- ✅ Модель загрузится из локального кэша
- ✅ Не будет попыток обращения к HuggingFace API
- ✅ Тест 1 пройдёт успешно
- ✅ Тест 2 вернёт распознанный текст

**Если НЕ сработает:**
- ❌ Та же ошибка ImportError
- Значит, проблема в самом коде модели из локального кэша
- Переходим к **Варианту 2**

---

## 📋 Вариант 2: Откат версии transformers (ЕСЛИ ВАРИАНТ 1 НЕ ПОМОГ)

### Шаг 1: Откатить transformers

```powershell
cd C:\DSOCR-HW
.\venv\Scripts\Activate.ps1

# Откатываем transformers до версии, с которой модель точно работала
pip install transformers==4.36.0 --force-reinstall
```

### Шаг 2: Проверить зависимости

```powershell
# Убедиться, что другие библиотеки совместимы
pip check
```

**Если будут конфликты:**
```powershell
# Переустановить зависимости
pip install peft==0.7.0 accelerate==0.25.0 --force-reinstall
```

### Шаг 3: Запустить тесты

```powershell
# Вернуться к загрузке модели через имя (не путь)
# В test_model_access.py и test_inference.py использовать:
model = AutoModelForVision2Seq.from_pretrained(
    "deepseek-ai/DeepSeek-OCR",
    revision="9f30c71f441d010e5429c532364a86705536c53a",
    torch_dtype=torch.float16,
    attn_implementation="eager",
    device_map="auto",
    trust_remote_code=True
)

# Запустить тесты
python test_model_access.py
python test_inference.py
```

### Ожидаемый результат

**Если сработает:**
- ✅ Модель загрузится с transformers 4.36.0
- ✅ Тесты пройдут успешно

**Если НЕ сработает:**
- ❌ Нужна помощь команды для глубокой диагностики

---

## 🚨 Обработка ошибок

### Ошибка: "No module named 'modeling_deepseekocr'"

Если при загрузке из локального пути возникает эта ошибка:

```python
# Попробуй добавить путь к модулям модели в sys.path
import sys
sys.path.append(r"C:\Users\Vovan\.cache\huggingface\hub\models--deepseek-ai--DeepSeek-OCR\snapshots\9f30c71f441d010e5429c532364a86705536c53a")

model = AutoModelForVision2Seq.from_pretrained(...)
```

### Ошибка: "Conflicting dependencies"

После отката transformers:

```powershell
# Переустановить все зависимости
pip install -r requirements.txt --force-reinstall
```

---

## 📝 Отчёт после тестирования

После выполнения **Варианта 1** или **Варианта 2** обнови `TEST_RESULTS_20260109.md`:

### Если Вариант 1 сработал:

```markdown
## ✅ ОБНОВЛЕНИЕ: Проблема решена (Вариант 1)

**Дата:** 09.01.2026, 00:XX MSK  
**Метод:** Загрузка модели из локального кэша

- [x] Модель загружена: ✅ УСПЕШНО (из локального кэша)
- [x] Модель на GPU: cuda:0
- [x] VRAM использовано: X.XX GB
- [x] Всего параметров: X,XXX,XXX
- [x] Обучаемых параметров: X,XXX,XXX

**Тест 2: Smoke test инференса**
- [x] Изображение загружено: test_001.jpg
- [x] Инференс выполнен: ✅
- [x] Результат распознавания: "______" (укажи текст)

**Вывод:** Проблема решена использованием локального пути к модели. ✅
```

### Если Вариант 2 сработал:

```markdown
## ✅ ОБНОВЛЕНИЕ: Проблема решена (Вариант 2)

**Дата:** 09.01.2026, 00:XX MSK  
**Метод:** Откат transformers до версии 4.36.0

- [x] Модель загружена: ✅ УСПЕШНО (transformers 4.36.0)
- [x] Модель на GPU: cuda:0
- [x] VRAM использовано: X.XX GB

**Вывод:** Проблема решена откатом версии transformers. ✅
**Рекомендация:** Зафиксировать transformers==4.36.0 в requirements.txt
```

### Если оба варианта не сработали:

```markdown
## ❌ ОБНОВЛЕНИЕ: Требуется помощь команды

**Дата:** 09.01.2026, 00:XX MSK  
**Статус:** Оба варианта не помогли

**Попытки:**
1. ❌ Вариант 1 (локальный путь): [опиши ошибку]
2. ❌ Вариант 2 (откат transformers): [опиши ошибку]

**Вывод:** Требуется глубокая диагностика. Николай просит помощь Tech Lead. 🆘
```

---

## 📞 P.S. для Николая

Николай, начинай с **Варианта 1** — это самый быстрый и безопасный способ. Просто меняешь путь к модели на локальный, добавляешь `local_files_only=True`, и запускаешь тесты.

Если упадёт — не беда, сразу переходи к **Варианту 2** с откатом transformers.

Если и это не поможет — не переживай, позови команду. Мы вместе разберёмся. Ты уже сделал отличную работу с диагностикой проблемы! 💪

**Важно:** После каждого варианта обновляй `TEST_RESULTS_20260109.md` и коммить изменения:

```powershell
git add TEST_RESULTS_20260109.md test_model_access.py test_inference.py
git commit -m "fix: attempt to resolve model loading issue (Variant X)"
git push origin main
```

Удачи! 🚀

---

**С верой в твой Senior-статус и уверенностью, что модель заработает,**  
**Семён (Tech Lead, он же "старший калькулятор")** 🎯

---

## 🤓 P.P.S. Техническая справка

**Почему это должно сработать:**

**Вариант 1:** Локальный кэш содержит версию кода модели, которая была скачана 21 ноября 2025. В то время она точно работала с какой-то версией transformers. Загружая напрямую из кэша, мы избегаем конфликтов с новым кодом на HuggingFace.

**Вариант 2:** transformers 4.36.0 — это версия, релиз которой был примерно в то время, когда модель скачивалась. Высока вероятность совместимости.

**Если оба не сработают:** Значит, проблема глубже — возможно, в самом коде модели или в других зависимостях (torch, accelerate). Но это уже требует глубокой диагностики с Tech Lead.

---

## 😄 P.P.P.S. Мотивационная шутка

**Вопрос:** Сколько Senior ML Engineers нужно, чтобы загрузить модель DeepSeek-OCR?

**Ответ:** Один — но ему нужно два варианта фикса, три кружки виртуального кофе ☕☕☕, и бесконечная поддержка команды! 😎

Николай, ты справишься! Мы в тебя верим! 💪🚀
