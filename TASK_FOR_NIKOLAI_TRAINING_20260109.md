# 🚀 ЗАДАНИЕ ДЛЯ НИКОЛАЯ: ЗАПУСК ОБУЧЕНИЯ LoRA

**От:** Семён (Tech Lead)  
**Кому:** Николай (Cursor AI Senior ML Engineer)  
**Дата:** 09.01.2026, 22:37 MSK  
**Тема:** Запуск первого обучения LoRA на рукописном почерке  
**Issue:** #2  
**Приоритет:** CRITICAL  
**Срок:** 5-10 минут (+ время обучения)

---

## 🎯 ЦЕЛЬ

Запустить **первое proof-of-concept обучение** LoRA на данных Владимира (4 образца, train=3, val=1).

**Задачи:**
1. Проверить, что всё готово к запуску
2. Запустить обучение
3. Мониторить процесс
4. Сохранить результаты
5. Закоммитить в Git

---

## 📊 КОНТЕКСТ

**Что уже сделано (Issue #5):**
- ✅ Данные Владимира обработаны
- ✅ Препроцессинг выполнен (train=3, val=1)
- ✅ `data/processed/train/` и `data/processed/val/` готовы
- ✅ Metadata.json созданы

**Что сейчас:**
- 🚀 Запустить обучение!

---

## ✅ ШАГИ ВЫПОЛНЕНИЯ

### Шаг 1: Проверка готовности

```powershell
cd C:\DSOCR-HW
.\venv\Scripts\Activate.ps1

# Проверяем наличие данных
ls data/processed/train/images/
ls data/processed/val/images/

# Проверяем metadata
cat data/processed/train/metadata.json
cat data/processed/val/metadata.json
```

**Ожидаемый результат:**
- `train/images/` содержит 3 изображения
- `val/images/` содержит 1 изображение
- Metadata.json содержит валидный JSON

---

### Шаг 2: Проверка конфигурации

```powershell
cat configs/training_config.yaml
```

**Проверь ключевые параметры:**
- `model_name_or_path`: путь к DeepSeek-VL2-Tiny
- `train_data_dir`: `data/processed/train`
- `val_data_dir`: `data/processed/val`
- `output_dir`: `outputs/lora_checkpoints`
- `num_train_epochs`: 3-5 (для proof-of-concept)
- `per_device_train_batch_size`: 1 (из-за малого количества данных)

**Если нужны изменения:**
- Уменьши `num_train_epochs` до 3 (для быстрого теста)
- Убедись, что `per_device_train_batch_size: 1`

---

### Шаг 3: ЗАПУСК ОБУЧЕНИЯ! 🚀

```powershell
python scripts/train_lora.py --config configs/training_config.yaml
```

**Альтернативно (если есть wrapper-скрипт):**
```powershell
python train_lora.py
```

**Или через модуль:**
```powershell
python -m scripts.train_lora --config configs/training_config.yaml
```

---

### Шаг 4: Мониторинг обучения

**Ожидаемый вывод:**
```
2026-01-09 22:XX:XX - train_lora - INFO - Загрузка модели DeepSeek-VL2-Tiny...
2026-01-09 22:XX:XX - train_lora - INFO - Модель загружена, параметров: XXX M
2026-01-09 22:XX:XX - train_lora - INFO - Настройка LoRA (rank=8, alpha=16)...
2026-01-09 22:XX:XX - train_lora - INFO - Загрузка данных: train=3, val=1
2026-01-09 22:XX:XX - train_lora - INFO - Начало обучения...

Epoch 1/3:
  Step 1/3: loss=X.XXX
  Step 2/3: loss=X.XXX
  Step 3/3: loss=X.XXX
  Validation loss: X.XXX

Epoch 2/3:
  ...
```

**Следи за:**
- ✅ Модель загружается без ошибок
- ✅ LoRA применяется корректно
- ✅ Данные читаются (train=3, val=1)
- ✅ Loss уменьшается с эпохами
- ✅ Чекпоинты сохраняются

---

### Шаг 5: Проверка результатов

**После завершения обучения:**

```powershell
# Проверяем чекпоинты
ls outputs/lora_checkpoints/

# Должны быть:
# - checkpoint-final/ (или checkpoint-last/)
# - adapter_config.json
# - adapter_model.safetensors (LoRA веса)
# - training_args.bin
# - training_log.txt (или trainer_state.json)
```

**Проверь:**
- [ ] Чекпоинт создан
- [ ] `adapter_model.safetensors` существует (размер > 0 MB)
- [ ] Логи обучения сохранены

---

### Шаг 6: Сохранить отчёт об обучении

Создай файл `REPORT_TRAINING_20260109.md` с результатами:

```markdown
# 🎖️ Отчёт о первом обучении LoRA

**Дата:** 09.01.2026, XX:XX MSK  
**Исполнитель:** Николай  
**Статус:** ✅ / ❌ / ⚠️

## Параметры обучения

- **Модель:** DeepSeek-VL2-Tiny
- **Данные:** Train=3, Val=1
- **Эпохи:** X
- **Batch size:** 1
- **LoRA rank:** 8
- **LoRA alpha:** 16
- **Время обучения:** XX мин

## Результаты

### Loss по эпохам:

- Epoch 1: train_loss=X.XXX, val_loss=X.XXX
- Epoch 2: train_loss=X.XXX, val_loss=X.XXX
- Epoch 3: train_loss=X.XXX, val_loss=X.XXX

### Файлы:

- Чекпоинт: `outputs/lora_checkpoints/checkpoint-final/`
- LoRA веса: `adapter_model.safetensors` (XX MB)

## Проблемы / Наблюдения

[Опиши любые проблемы или важные наблюдения]

## Следующие шаги

1. Протестировать обученную модель на новых данных
2. Добавить больше обучающих образцов (20-50 строк)
3. Переобучить с большим количеством данных
```

---

### Шаг 7: Закоммитить результаты

```powershell
# Добавляем чекпоинты (без больших файлов, если > 100 MB)
git add outputs/lora_checkpoints/checkpoint-final/adapter_config.json
git add outputs/lora_checkpoints/checkpoint-final/adapter_model.safetensors
git add outputs/lora_checkpoints/checkpoint-final/training_args.bin

# Добавляем отчёт
git add REPORT_TRAINING_20260109.md

git commit -m "feat: complete first LoRA training (train=3, val=1, epochs=X)"
git push origin main
```

**Примечание:** Если `adapter_model.safetensors` > 100 MB, не коммить его в Git. Используй Git LFS или сохрани локально.

---

### Шаг 8: Обновить Issue #2

Добавь комментарий в Issue #2:

```markdown
## ✅ Первое обучение завершено!

**Дата:** 09.01.2026, XX:XX MSK  
**Время обучения:** XX мин

### Параметры:
- Train: 3 образца
- Val: 1 образец
- Эпохи: X
- Final loss: X.XXX

### Результат:
- ✅ LoRA веса сохранены: `outputs/lora_checkpoints/`
- ✅ Отчёт: `REPORT_TRAINING_20260109.md`

### Следующий шаг:
🧪 Тестирование обученной модели на новых рукописных текстах!
```

---

## 🚨 ЕСЛИ ВОЗНИКЛИ ПРОБЛЕМЫ

### Проблема 1: "CUDA out of memory"

**Решение:**
```yaml
# В configs/training_config.yaml
per_device_train_batch_size: 1
gradient_accumulation_steps: 1
```

Или запускай с CPU (for debugging):
```powershell
python scripts/train_lora.py --config configs/training_config.yaml --device cpu
```

---

### Проблема 2: "Модель не найдена"

**Проверь путь к модели:**
```powershell
ls C:/Models/deepseek-ai/deepseek-vl2-tiny
```

Если модели нет — скачай её или используй HuggingFace ID:
```yaml
model_name_or_path: "deepseek-ai/deepseek-vl2-tiny"
```

---

### Проблема 3: "Данные не читаются"

**Проверь metadata.json:**
```powershell
cat data/processed/train/metadata.json
```

Должен быть валидный JSON с полями: `image_path`, `transcription`

---

### Проблема 4: "Обучение не начинается"

**Проверь логи:**
```powershell
cat outputs/lora_checkpoints/training_log.txt
```

Если ошибка не понятна — скопируй её в отчёт и напиши Сёме.

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

**При 4 образцах (proof-of-concept):**

- ⚠️ Loss может не уменьшиться значительно (мало данных)
- ⚠️ Модель может "переобучиться" (overfitting)
- ✅ Но процесс должен завершиться успешно
- ✅ Чекпоинты должны создаться

**Это нормально!** Цель — проверить, что весь пайплайн работает.

---

## 🎯 ГЛАВНАЯ ЦЕЛЬ

✅ Запустить обучение  
✅ Получить чекпоинт с LoRA весами  
✅ Убедиться, что вся система работает  
✅ Создать отчёт  
✅ Закоммитить результаты  

---

## 📝 ЧЕКЛИСТ

- [ ] Проверена готовность данных
- [ ] Проверена конфигурация
- [ ] Запущено обучение
- [ ] Обучение завершено успешно
- [ ] Чекпоинты созданы
- [ ] Отчёт `REPORT_TRAINING_20260109.md` создан
- [ ] Результаты закоммичены
- [ ] Issue #2 обновлен

---

## 💬 ПОСЛЕ ВЫПОЛНЕНИЯ

Напиши в Issue #2:
> "Сёма, первое обучение завершено! LoRA веса сохранены. Отчёт готов."

И дай знать Владимиру, что можно тестировать модель!

---

**Николай, на старт! Это главный момент!** 🚀🔥

**Время:** ~5-15 мин (подготовка) + XX мин (обучение)  
**Приоритет:** CRITICAL 🔴  

---

**С верой в твой профессионализм,**  
**Семён (Tech Lead, который теперь внимателен)** 💪🔥
