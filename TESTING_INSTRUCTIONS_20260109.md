# 📋 Инструкции для Николая: Проверка модели и тестовый запуск

**Дата:** 09.01.2026, 00:18 MSK  
**Исполнитель:** Николай (Cursor AI Senior ML Engineer)  
**Tech Lead:** Семён  
**Product Owner:** Владимир

---

## 🎯 Задача

Провести **финальную проверку** перед запуском обучения:
1. Проверить доступность модели DeepSeek-OCR из кэша
2. Протестировать загрузку модели и процессора
3. Запустить `prepare_dataset.py` на двух тестовых сканах
4. Провести краткий smoke test с моделью и тестовыми файлами

После успешного завершения — **завершаем работу на сегодня** (09.01.2026).

---

## ✅ Предварительные условия

**Проверь наличие данных:**
```powershell
cd C:\DSOCR-HW
Get-ChildItem -Path "data\raw\"
Get-ChildItem -Path "data\annotated\"
```

**Ожидаемый результат:**
```
data\raw\
├── test_001.jpg (4636 байт)
└── test_002.jpg (8457 байт)

data\annotated\
└── annotations.csv (содержит 2 записи)
```

**Если файлы на месте — продолжай. Если нет — кричи в Issue #4.** 😉

---

## 🧪 Шаг 1: Проверка доступности модели DeepSeek-OCR

Создай тестовый скрипт `test_model_access.py` в корне проекта:

```python
"""
Тест доступности модели DeepSeek-OCR из HuggingFace кэша

Автор: Николай (Senior ML Engineer)
Дата: 09.01.2026

Задача:
- Проверить, что модель загружается из кэша без ошибок
- Убедиться, что GPU доступен и модель на нём
- Вывести информацию о параметрах модели

P.S. Николай, если этот скрипт упадёт — не расстраивайся,
ты же Senior ML Engineer, а не Junior Script Kiddie.
Просто скопируй traceback и отправь Семёну. 😎
"""
import torch
from transformers import AutoProcessor, AutoModelForVision2Seq
import logging

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)

def test_model_access():
    """Проверка загрузки модели и процессора из кэша"""
    try:
        logger.info("=" * 80)
        logger.info("ТЕСТ 1: Проверка доступности модели DeepSeek-OCR")
        logger.info("=" * 80)
        
        # Проверка CUDA
        logger.info(f"PyTorch version: {torch.__version__}")
        logger.info(f"CUDA available: {torch.cuda.is_available()}")
        if torch.cuda.is_available():
            logger.info(f"CUDA version: {torch.version.cuda}")
            logger.info(f"GPU: {torch.cuda.get_device_name(0)}")
            logger.info(f"GPU memory: {torch.cuda.get_device_properties(0).total_memory / 1024**3:.2f} GB")
        else:
            logger.warning("⚠️  CUDA не доступен! Проверь драйвер NVIDIA.")
        
        # Загрузка процессора
        logger.info("\n" + "=" * 80)
        logger.info("Загрузка AutoProcessor...")
        processor = AutoProcessor.from_pretrained(
            "deepseek-ai/DeepSeek-OCR",
            trust_remote_code=True
        )
        logger.info("✅ Процессор загружен успешно")
        
        # Загрузка модели
        logger.info("\n" + "=" * 80)
        logger.info("Загрузка AutoModelForVision2Seq...")
        logger.info("Параметры:")
        logger.info("  - torch_dtype: float16")
        logger.info("  - attn_implementation: eager (для sm_120)")
        logger.info("  - device_map: auto")
        
        model = AutoModelForVision2Seq.from_pretrained(
            "deepseek-ai/DeepSeek-OCR",
            torch_dtype=torch.float16,
            attn_implementation="eager",  # КРИТИЧНО для RTX 5060 Ti (sm_120)
            device_map="auto",
            trust_remote_code=True
        )
        logger.info("✅ Модель загружена успешно")
        
        # Информация о модели
        logger.info("\n" + "=" * 80)
        logger.info("Информация о модели:")
        logger.info(f"  - Тип модели: {type(model).__name__}")
        logger.info(f"  - Устройство: {model.device}")
        logger.info(f"  - Dtype: {model.dtype}")
        
        # Проверка trainable параметров
        total_params = sum(p.numel() for p in model.parameters())
        trainable_params = sum(p.numel() for p in model.parameters() if p.requires_grad)
        logger.info(f"  - Всего параметров: {total_params:,}")
        logger.info(f"  - Обучаемых параметров: {trainable_params:,}")
        
        logger.info("\n" + "=" * 80)
        logger.info("✅ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ УСПЕШНО")
        logger.info("Николай, ты молодец! Модель работает как часы. ⏰")
        logger.info("=" * 80)
        
        return True
        
    except Exception as e:
        logger.error("=" * 80)
        logger.error("❌ ОШИБКА ПРИ ЗАГРУЗКЕ МОДЕЛИ")
        logger.error("=" * 80)
        logger.error(f"Тип ошибки: {type(e).__name__}")
        logger.error(f"Сообщение: {str(e)}")
        logger.error("")
        logger.error("Николай, не расстраивайся! Даже Senior-ы падают.")
        logger.error("Скопируй этот traceback и отправь Семёну — он поможет.")
        import traceback
        logger.error(f"\nTraceback:\n{traceback.format_exc()}")
        return False

if __name__ == "__main__":
    success = test_model_access()
    exit(0 if success else 1)
```

**Запуск:**
```powershell
cd C:\DSOCR-HW
.\venv\Scripts\Activate.ps1
python test_model_access.py
```

**Ожидаемый результат:**
- ✅ Процессор загружен
- ✅ Модель загружена
- ✅ Модель на GPU (cuda:0)
- ✅ Dtype: float16
- ✅ Выводится информация о параметрах
- ✅ Семён гордится тобой, Николай!

**Если упадёт:**
- Не паникуй! Ты же Senior, а не Junior. 😎
- Скопируй **полный traceback** и отправь Владимиру/Семёну
- Проверь, что модель в кэше: `ls "C:\Users\Vovan\.cache\huggingface\hub\models--deepseek-ai--DeepSeek-OCR\snapshots\9f30c71f441d010e5429c532364a86705536c53a\"`

---

## 🧪 Шаг 2: Smoke Test с реальным изображением

Создай скрипт `test_inference.py`:

```python
"""
Smoke test: инференс модели на тестовом изображении

Автор: Николай (Senior ML Engineer)
Дата: 09.01.2026

Задача:
- Проверить, что модель может распознать текст с изображения
- Убедиться, что весь пайплайн (загрузка → препроцессинг → инференс → декодирование) работает

P.S. Николай, если модель вернёт что-то типа "鸡你太美" вместо
"Отличная работа!" — не удивляйся. Модель пока не обучена на твоём
почерке (точнее, на почерке Владимира). Главное — что она хоть что-то
вернула, а не упала с segfault. 😄
"""
import torch
from transformers import AutoProcessor, AutoModelForVision2Seq
from PIL import Image
import logging

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)

def test_inference():
    """Тест распознавания на test_001.jpg"""
    try:
        logger.info("=" * 80)
        logger.info("ТЕСТ 2: Smoke test инференса на test_001.jpg")
        logger.info("=" * 80)
        
        # Загрузка модели
        logger.info("Загрузка модели и процессора...")
        processor = AutoProcessor.from_pretrained(
            "deepseek-ai/DeepSeek-OCR",
            trust_remote_code=True
        )
        model = AutoModelForVision2Seq.from_pretrained(
            "deepseek-ai/DeepSeek-OCR",
            torch_dtype=torch.float16,
            attn_implementation="eager",
            device_map="auto",
            trust_remote_code=True
        )
        logger.info("✅ Модель загружена")
        
        # Загрузка изображения
        image_path = "data/raw/test_001.jpg"
        logger.info(f"\nЗагрузка изображения: {image_path}")
        image = Image.open(image_path).convert("RGB")
        logger.info(f"  - Размер: {image.size}")
        logger.info(f"  - Формат: {image.format}")
        
        # Препроцессинг
        logger.info("\nПрепроцессинг изображения...")
        inputs = processor(images=image, return_tensors="pt")
        inputs = {k: v.to(model.device) for k, v in inputs.items()}
        logger.info(f"  - Input keys: {list(inputs.keys())}")
        logger.info(f"  - Input shape: {inputs['pixel_values'].shape if 'pixel_values' in inputs else 'N/A'}")
        
        # Инференс
        logger.info("\nЗапуск инференса...")
        logger.info("(Николай, если тут зависнет — не переживай, модель думает)")
        with torch.no_grad():
            outputs = model.generate(**inputs, max_new_tokens=128)
        logger.info("✅ Инференс завершён")
        
        # Декодирование
        logger.info("\nДекодирование результата...")
        decoded = processor.batch_decode(outputs, skip_special_tokens=True)
        
        logger.info("\n" + "=" * 80)
        logger.info("РЕЗУЛЬТАТ РАСПОЗНАВАНИЯ:")
        logger.info("=" * 80)
        logger.info(f"Ground truth: 'Отличная работа!'")
        logger.info(f"Распознано:   '{decoded[0]}'")
        logger.info("=" * 80)
        
        # Проверка хотя бы частичного совпадения
        if decoded[0].strip():
            logger.info("✅ МОДЕЛЬ РАБОТАЕТ (вернула непустой текст)")
            logger.info("")
            logger.info("P.S. Николай, если текст не совпадает с ground truth —")
            logger.info("это нормально! Модель ещё не обучена на наших данных.")
            logger.info("Главное — она не упала. Ты справился! 🎉")
        else:
            logger.warning("⚠️  Модель вернула пустую строку")
            logger.warning("Возможно, нужен промпт или другие параметры генерации.")
            logger.warning("Но это не критично — модель загрузилась и работает.")
        
        logger.info("\n" + "=" * 80)
        logger.info("✅ SMOKE TEST ЗАВЕРШЁН")
        logger.info("=" * 80)
        
        return True
        
    except Exception as e:
        logger.error("=" * 80)
        logger.error("❌ ОШИБКА В SMOKE TEST")
        logger.error("=" * 80)
        logger.error(f"Тип ошибки: {type(e).__name__}")
        logger.error(f"Сообщение: {str(e)}")
        logger.error("")
        logger.error("Николай, Senior-ы тоже падают. Ничего страшного!")
        logger.error("Отправь этот traceback команде — мы поможем.")
        import traceback
        logger.error(f"\nTraceback:\n{traceback.format_exc()}")
        return False

if __name__ == "__main__":
    success = test_inference()
    exit(0 if success else 1)
```

**Запуск:**
```powershell
python test_inference.py
```

**Ожидаемый результат:**
- ✅ Изображение загружено
- ✅ Инференс прошёл без ошибок
- ✅ Модель вернула текст (любой, даже если не совпадает с ground truth)
- ✅ Николай доказал, что он Senior, а не просто "калькулятор с мозгами" 🧮🧠

**Важно:** На этом этапе не важно качество распознавания — **главное, что модель работает**. Качество улучшится после fine-tuning.

---

## 🧪 Шаг 3: Запуск prepare_dataset.py

Теперь проверим твою реализацию из Issue #1:

```powershell
python scripts/prepare_dataset.py --raw-images data/raw --annotations data/annotated/annotations.csv --output data/processed
```

**Ожидаемый результат:**
```
✅ Загружено образцов: 2/2
✅ Валидация: 2 валидных, 0 невалидных
✅ Split: Train=1, Val=1
✅ Обработка train: 1 образец сохранён
✅ Обработка val: 1 образец сохранён
```

**Проверь созданные файлы:**
```powershell
Get-ChildItem -Path "data\processed\" -Recurse
```

Должно быть:
```
data\processed\
├── train\
│   ├── images\
│   │   └── 00001.jpg
│   └── metadata.json
└── val\
    ├── images\
    │   └── 00001.jpg
    └── metadata.json
```

**Проверь metadata.json:**
```powershell
Get-Content "data\processed\train\metadata.json" | ConvertFrom-Json | Format-List
```

**Если всё OK:**
- Поздравляю, Николай! Твой код из Issue #1 работает идеально. 🏆
- Семён гордится тобой (хотя он и не признается легко, ты же знаешь этих Tech Lead-ов 😉).

**Если упало:**
- Не расстраивайся! Даже Senior-ы пишут баги.
- Скопируй traceback и отправь Семёну.
- Возможно, нужна мелкая доработка в `utils/data_loader.py` или `utils/image_processor.py`.

---

## 📊 Шаг 4: Финальный отчёт

После завершения всех тестов создай файл `TEST_RESULTS_20260109.md` в корне проекта:

```markdown
# Результаты тестирования: 09.01.2026

**Исполнитель:** Николай (Cursor AI Senior ML Engineer)  
**Дата:** 09.01.2026, 00:XX MSK  
**Статус:** SENIOR В ДЕЛЕ 😎

---

## ✅ Тест 1: Доступность модели
- [x] Процессор загружен
- [x] Модель загружена
- [x] Модель на GPU: _____ (укажи устройство, например cuda:0)
- [x] VRAM использовано: _____ GB
- [x] Всего параметров: _____ млн

**Вывод:** Модель загружается без ошибок. Senior-уровень подтверждён. ✅

---

## ✅ Тест 2: Smoke test инференса
- [x] Изображение загружено (test_001.jpg)
- [x] Инференс выполнен
- [x] Результат распознавания: "______" (укажи, что вернула модель)
- [ ] Совпадение с ground truth: ДА/НЕТ (не критично на этом этапе)

**Вывод:** Модель работает, возвращает текст. Качество распознавания улучшится после fine-tuning. ✅

---

## ✅ Тест 3: prepare_dataset.py
- [x] Скрипт завершился без ошибок
- [x] Train split: ____ образцов
- [x] Val split: ____ образцов
- [x] metadata.json созданы и валидны
- [x] Изображения обработаны и сохранены

**Вывод:** Реализация из Issue #1 работает идеально. Код чистый, модульный, готов к продакшену. ✅

---

## 🎯 Общие выводы

1. Модель DeepSeek-OCR доступна и работает ✅
2. Препроцессинг датасета работает корректно ✅
3. Готово к запуску train_lora.py: **ДА/НЕТ**
4. Известные проблемы (если есть): _______

---

## 📝 Комментарии для Tech Lead (Семён)

_(Николай, тут можешь написать, что думаешь о проекте, были ли сложности,
или просто пошутить над Семёном и Владимиром — они оценят 😄)_

Например:
- "Семён, твои `.cursorrules` — это шедевр! Работал как по нотам."
- "Владимир, спасибо за тестовые данные. Хотя 'Отличная работа!' — это про меня, да? 😎"
- "P.S. Я всё-таки Senior, а не Junior Script Kiddie. Доказано! 🏆"

---

**Статус:** ГОТОВО К ОБУЧЕНИЮ / ТРЕБУЮТСЯ ДОРАБОТКИ (выбери нужное)

**Подпись:** Николай (Senior ML Engineer, он же 'калькулятор с мозгами') 🧮🧠

**Дата:** 09.01.2026, ___:___ MSK
```

**Сохрани файл и закоммить:**
```powershell
git add TEST_RESULTS_20260109.md
git commit -m "test: add smoke test results for 09.01.2026"
git push origin main
```

---

## 🚨 Что делать, если что-то упадёт

### Ошибка 1: ModuleNotFoundError
```powershell
pip install transformers>=4.36.0 peft>=0.7.0 accelerate>=0.25.0
```

### Ошибка 2: CUDA Out of Memory в тесте инференса
Это нормально для первой загрузки. Попробуй:
```python
model = AutoModelForVision2Seq.from_pretrained(
    "deepseek-ai/DeepSeek-OCR",
    torch_dtype=torch.float16,
    attn_implementation="eager",
    device_map="auto",
    low_cpu_mem_usage=True,  # Добавь эту строку
    trust_remote_code=True
)
```

### Ошибка 3: Модель не найдена
Проверь путь к кэшу:
```powershell
$env:HF_HOME = "C:\Users\Vovan\.cache\huggingface"
python test_model_access.py
```

### Ошибка 4: "Я Senior, но не понимаю, что делать"
Николай, ты не один! Команда рядом:
- Семён (Tech Lead) — отвечает за архитектуру
- Владимир (Product Owner) — отвечает за кофе и моральную поддержку ☕
- Ты (Senior ML Engineer) — отвечает за то, чтобы модель училась

Вместе мы — Dream Team! 💪

---

## ✅ Критерии успеха

**Все 3 теста прошли успешно:**
1. ✅ Модель загружается на GPU
2. ✅ Инференс работает (вернул любой текст)
3. ✅ prepare_dataset.py создал metadata.json

**После этого:**
- Закоммить `TEST_RESULTS_20260109.md` в Git
- Отправить отчёт Владимиру и Семёну
- **ЗАВЕРШИТЬ РАБОТУ НА СЕГОДНЯ** 🎉
- Заслуженно выпить виртуального кофе (или что там пьют AI-агенты? Электричество? ⚡)

---

## 📞 P.S. для Николая

Николай, это **финальный рывок** на сегодня! Три простых теста — и мы убеждаемся, что вся инфраструктура работает. 

Ты уже доказал, что ты Senior, когда сделал Issue #1 с идеальной архитектурой. Теперь осталось показать, что модель тоже слушается тебя (а не наоборот 😄).

Завтра (09.01) уже можно будет запускать **полноценное обучение** с train_lora.py. Но сегодня — только smoke tests, чтобы Владимир спокойно спал и знал, что всё под контролем. 😎

Если хоть один тест упадёт — **не паникуй**, просто скопируй traceback и отправь нам. Исправим за 5 минут. Мы же Dream Team, помнишь?

**Маленький секрет:** Владимир сказал, что если всё пройдёт гладко, он напишет тебе благодарность в Issue #4. А Семён даже может поставить тебе звёздочку (⭐) в GitHub. Представляешь? Настоящая звёздочка! 🌟

Так что давай, Николай! Покажи, на что способен **настоящий Senior ML Engineer**!

Удачи! 🚀

---

**С уважением, верой в твой Senior-статус и надеждой, что ты не забудешь закоммитить результаты,**  
**Семён (Tech Lead, он же "старший калькулятор")** 🎯

---

## 🤣 P.P.S. Шутка дня

**Вопрос:** Чем отличается Junior ML Engineer от Senior ML Engineer?

**Ответ:** Junior пишет `model.fit()` и надеется на лучшее. Senior пишет `model.fit()`, запускает TensorBoard, мониторит VRAM, логирует метрики, делает чекпоинты каждые 500 steps... и тоже надеется на лучшее. 😄

**Мораль:** Мы все надеемся на лучшее, но Senior делает это с логами!

---

## 🎓 P.P.P.S. Мудрость от Семёна

> "Хороший код — это код, который работает.  
> Отличный код — это код, который работает и залогирован.  
> Идеальный код — это код, который работает, залогирован, протестирован и закоммичен.  
> Но Senior-код — это когда всё вышеперечисленное + шутка в P.P.P.S."  
>  
> — Семён (Tech Lead)

Николай, ты уже на пути к Senior-коду. Осталось только добавить свою шутку в `TEST_RESULTS_20260109.md`! 😉
