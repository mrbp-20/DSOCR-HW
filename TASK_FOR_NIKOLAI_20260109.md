# 🎯 ЗАДАНИЕ ДЛЯ НИКОЛАЯ (09.01.2026, 22:23 MSK)

**От:** Семён (Tech Lead)  
**Кому:** Николай (Cursor AI Senior ML Engineer)  
**Тема:** Обработка данных Владимира и подготовка к обучению  
**Issue:** #5  
**Приоритет:** HIGH  
**Срок:** 10-15 минут

---

## 📊 Контекст

Владимир подготовил данные для обучения:
- 4 изображения рукописного текста: `test_000.jpg`, `test_001.jpg`, `test_002.jpg`, `test_003.jpg`
- CSV файл с транскрипциями: `annotations.csv`

**Файлы уже в проекте** (в `C:\DSOCR-HW\data\`), но **не закоммичены в Git**.

Твоя задача: обработать эти данные и подготовить к запуску обучения.

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ (7 шагов)

### Шаг 1: Добавить исходные данные в Git

```powershell
cd C:\DSOCR-HW
git add data/raw/test_000.jpg
git add data/raw/test_001.jpg
git add data/raw/test_002.jpg
git add data/raw/test_003.jpg
git add data/annotations.csv
git commit -m "data: add training samples (4 images + annotations)"
git push origin main
```

**Проверка:** Файлы должны появиться на GitHub в папке `data/raw/`

---

### Шаг 2: Создать скрипт конвертера `convert_annotations.py`

Создай файл `convert_annotations.py` в корне проекта (`C:\DSOCR-HW\convert_annotations.py`) со следующим кодом:

```python
"""
Конвертер CSV из формата Владимира в формат для prepare_dataset.py

Преобразует:
- filename,text → image_path,transcription
- test_000.jpg → raw/test_000.jpg
"""
import pandas as pd

def convert_annotations(
    input_csv: str = "data/annotations.csv",
    output_csv: str = "data/raw_samples.csv"
):
    """
    Конвертирует CSV из формата (filename, text) 
    в формат (image_path, transcription) с добавлением raw/ префикса
    """
    print(f"🔄 Читаю {input_csv}...")
    
    # Читаем исходный CSV
    df = pd.read_csv(input_csv)
    
    # Проверяем наличие нужных колонок
    if 'filename' in df.columns and 'text' in df.columns:
        # Переименовываем колонки
        df = df.rename(columns={'filename': 'image_path', 'text': 'transcription'})
    else:
        print(f"⚠️  Найдены колонки: {list(df.columns)}")
        # Если колонки уже правильные, используем как есть
        if 'image_path' not in df.columns or 'transcription' not in df.columns:
            print("❌ Ошибка: неправильный формат CSV")
            return
    
    # Добавляем raw/ к путям (если ещё не добавлено)
    if not df['image_path'].str.startswith('raw/').all():
        df['image_path'] = 'raw/' + df['image_path']
    
    # Сохраняем
    df.to_csv(output_csv, index=False, encoding='utf-8')
    
    print(f"✅ Конвертация завершена!")
    print(f"   Входной файл: {input_csv}")
    print(f"   Выходной файл: {output_csv}")
    print(f"   Обработано записей: {len(df)}")
    print(f"\n📋 Первые 3 строки результата:")
    print(df.head(3).to_string(index=False))

if __name__ == "__main__":
    convert_annotations()
```

**Проверка:** Файл `convert_annotations.py` создан в корне проекта

---

### Шаг 3: Запустить конвертер

```powershell
cd C:\DSOCR-HW
.\venv\Scripts\Activate.ps1
python convert_annotations.py
```

**Ожидаемый вывод:**
```
🔄 Читаю data/annotations.csv...
✅ Конвертация завершена!
   Входной файл: data/annotations.csv
   Выходной файл: data/raw_samples.csv
   Обработано записей: 4

📋 Первые 3 строки результата:
           image_path                                     transcription
    raw/test_000.jpg  Миновало почти десятилетие с тех пор, как про...
    raw/test_001.jpg                                  Отличная работа!
    raw/test_002.jpg              Это лучшая команда, что я встречал.
```

**Проверка:** 
- Файл `data/raw_samples.csv` создан
- Содержит 4 записи
- Пути начинаются с `raw/`

---

### Шаг 4: Закоммитить скрипт и результат

```powershell
git add convert_annotations.py
git add data/raw_samples.csv
git commit -m "feat: add CSV converter for training data"
git push origin main
```

**Проверка:** Файлы `convert_annotations.py` и `data/raw_samples.csv` на GitHub

---

### Шаг 5: Запустить препроцессинг данных

```powershell
python prepare_dataset.py
```

**Ожидаемый вывод:**
```
2026-01-09 22:XX:XX - prepare_dataset - INFO - Загружено образцов: 4/4
2026-01-09 22:XX:XX - prepare_dataset - INFO - Валидация: 4 валидных, 0 невалидных
2026-01-09 22:XX:XX - prepare_dataset - INFO - Split: Train=3, Val=1
2026-01-09 22:XX:XX - prepare_dataset - INFO - Обработка train: 3 образца сохранены
2026-01-09 22:XX:XX - prepare_dataset - INFO - Обработка val: 1 образец сохранён
```

**Проверка:**
- Папка `data/processed/train/` содержит 3 изображения
- Папка `data/processed/val/` содержит 1 изображение
- Файлы `metadata.json` созданы в обеих папках

---

### Шаг 6: Закоммитить обработанные данные

```powershell
git add data/processed/
git commit -m "data: add preprocessed training samples (train=3, val=1)"
git push origin main
```

**Проверка:** Папка `data/processed/` на GitHub

---

### Шаг 7: Обновить Issue #5

Добавь комментарий в Issue #5 с отчётом:

```markdown
## ✅ Задание выполнено!

**Дата:** 09.01.2026, 22:XX MSK  
**Время выполнения:** ~XX минут

### Результаты:

1. ✅ Исходные данные закоммичены в Git
   - 4 изображения: `test_000.jpg` - `test_003.jpg`
   - CSV: `annotations.csv`

2. ✅ Скрипт `convert_annotations.py` создан и работает
   - Обработано записей: 4
   - Создан `raw_samples.csv`

3. ✅ Препроцессинг выполнен успешно
   - Train split: 3 образца
   - Val split: 1 образец
   - Metadata.json созданы

4. ✅ Всё закоммичено в Git и залито на GitHub

### Следующий шаг:

🚀 Готово к запуску обучения LoRA (Issue #2)!
```

---

## 📋 ЧЕКЛИСТ ДЛЯ ПРОВЕРКИ

После выполнения всех шагов, проверь:

- [ ] На GitHub есть `data/raw/test_000.jpg` - `test_003.jpg`
- [ ] На GitHub есть `data/annotations.csv`
- [ ] На GitHub есть `convert_annotations.py`
- [ ] На GitHub есть `data/raw_samples.csv`
- [ ] На GitHub есть `data/processed/train/` (3 изображения)
- [ ] На GitHub есть `data/processed/val/` (1 изображение)
- [ ] Issue #5 обновлен с отчётом

---

## 🚨 Если что-то пошло не так

### Проблема: "pandas not found"

```powershell
pip install pandas
```

### Проблема: "File not found: data/annotations.csv"

Проверь, что файл существует:
```powershell
ls data/annotations.csv
```

Если нет — попроси Владимира указать правильный путь.

### Проблема: "Git push rejected"

```powershell
git pull origin main
git push origin main
```

### Проблема: Препроцессинг падает

Проверь формат `raw_samples.csv`:
```powershell
cat data/raw_samples.csv
```

Должно быть:
```csv
image_path,transcription
raw/test_000.jpg,"Текст..."
```

---

## 💬 После выполнения

Напиши в Issue #5:
> "Сёма, задание выполнено! Всё готово к обучению."

И дай знать Владимиру, что можно проверять результаты.

---

## 🎯 Цель задания

Подготовить данные Владимира (4 образца) для запуска обучения LoRA.

После выполнения этого задания мы сможем запустить:
```powershell
python train_lora.py
```

И начать первое обучение модели на рукописном почерке! 🚀

---

**Николай, на старт! Вперёд!** 💪

**Срок:** 10-15 минут  
**Приоритет:** HIGH (блокирует запуск обучения)

---

**С верой в твой Senior-статус,**  
**Семён (Tech Lead)** 🎯
