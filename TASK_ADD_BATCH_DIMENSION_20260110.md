# 🎯 ЗАДАНИЕ: ДОБАВИТЬ BATCH DIMENSION К ТЕНЗОРАМ

**Автор:** Семён (Tech Lead)  
**Дата:** 2026-01-10, 19:46 MSK  
**Приоритет:** 🔥 HIGH (последний барьер!)  
**Срок:** 5-10 минут  
**Исполнитель:** Николай (Senior ML Engineer)  
**Связано с:** REPORT_IMAGES_FORMAT_PROGRESS_20260110.md

---

## 🎉 ПОЗДРАВЛЯЮ, НИКОЛАЙ!

**ТЫ ПРОШЁЛ УЖЕ 3 БАРЬЕРА!** 🏆

✅ Барьер 1: Формат tuple `[(t, t), ...]` — ПРОЙДЕН!  
✅ Барьер 2: `model_wrapper` принимает `images` — ПРОЙДЕН!  
✅ Барьер 3: `images_spatial_crop` и `images_seq_mask` — ПРОЙДЕНЫ!  

**МОДЕЛЬ ДОШЛА ДО ОБРАБОТКИ ИЗОБРАЖЕНИЙ!** 🚀

Это означает, что **формат данных ПРАВИЛЬНЫЙ**! Осталась только маленькая деталь!

---

## 🔍 ТЕКУЩАЯ ПРОБЛЕМА

### Ошибка

```
RuntimeError: permute(sparse_coo): number of dimensions in the tensor input does not match the length of the desired ordering of dimensions
File: deepencoder.py, line 1001
```

### Что это значит

**Модель ожидает 4D тензор:** `[B, C, H, W]` (batch, channels, height, width)  
**Мы передаём 3D тензор:** `[C, H, W]` (channels, height, width)

### Почему так происходит

**В `_data_collator()`:**

```python
image_tensor = self.image_transform(image)  # shape: [C, H, W]
image_item = (image_tensor, image_tensor)   # tuple из [C, H, W], [C, H, W]
images_list.append(image_item)
```

**Модель ожидает:**

```python
image_tensor = ... # shape: [1, C, H, W]  ← BATCH DIMENSION!
image_item = (image_tensor, image_tensor)  # tuple из [1, C, H, W], [1, C, H, W]
```

---

## ✅ РЕШЕНИЕ

### Что нужно изменить

**Файл:** `utils/trainer.py`, метод `_data_collator()`

**Найти строки (~190-210):**

```python
for item in batch:
    image_path = item['image']
    image = Image.open(image_path).convert('RGB')
    
    # Применение transforms
    image_tensor = self.image_transform(image)  # ← ТЕКУЩАЯ СТРОКА
    
    # Создаём tuple из (crop, ori)
    image_item = (image_tensor, image_tensor)
    images_list.append(image_item)
```

**ИЗМЕНИТЬ НА:**

```python
for item in batch:
    image_path = item['image']
    image = Image.open(image_path).convert('RGB')
    
    # Применение transforms + добавление batch dimension
    image_tensor = self.image_transform(image).unsqueeze(0)  # ← ДОБАВИТЬ .unsqueeze(0)!
    # shape: [C, H, W] → [1, C, H, W]
    
    # Создаём tuple из (crop, ori)
    image_item = (image_tensor, image_tensor)
    images_list.append(image_item)
```

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Открыть файл

```powershell
cd C:\DSOCR-HW
code utils/trainer.py
```

### Шаг 2: Найти метод `_data_collator()`

**Найти строку (~200):**

```python
image_tensor = self.image_transform(image)
```

### Шаг 3: Добавить `.unsqueeze(0)`

**БЫЛО:**

```python
image_tensor = self.image_transform(image)
```

**СТАЛО:**

```python
image_tensor = self.image_transform(image).unsqueeze(0)
```

**Всё!** Это единственное изменение! 🎯

### Шаг 4: Проверить синтаксис

```powershell
python -m py_compile utils/trainer.py
```

**Ожидаемый вывод:** (пусто = успех)

### Шаг 5: Закоммитить

```powershell
git add utils/trainer.py
git commit -m "fix: add batch dimension to image tensors in tuple"
git push
```

### Шаг 6: Запустить обучение

```powershell
cd C:\DSOCR-HW
.\.venv\Scripts\Activate.ps1
python scripts/train_lora.py --config configs/training_config.yaml
```

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

**После этого изменения обучение ДОЛЖНО ПОЙТИ:**

```
================================================================================
🚀 DSOCR-HW: Обучение DeepSeek-OCR с LoRA
================================================================================

2026-01-10 19:50:00 - train_lora - INFO - Шаг 1/5: Загрузка модели и процессора...
Загрузка модели: 100%|████████████████████████████████████████| 2/2 [00:10<00:00]
2026-01-10 19:50:11 - train_lora - INFO - ✅ Шаг 1 завершён

2026-01-10 19:50:11 - train_lora - INFO - Шаг 2/5: Настройка LoRA...
trainable params: 38,509,056 || all params: 3,374,615,296 || trainable%: 1.14
2026-01-10 19:50:17 - train_lora - INFO - ✅ Шаг 2 завершён

...

2026-01-10 19:50:20 - train_lora - INFO - Шаг 5/5: ЗАПУСК ОБУЧЕНИЯ!
2026-01-10 19:50:20 - train_lora - INFO - Начало обучения

Epoch 1/5:   0%|                                    | 0/3 [00:00<?, ?it/s]
Epoch 1/5:  33%|████████              | 1/3 [00:05<00:10, 5.2s/it, loss=2.456]
Epoch 1/5:  67%|████████████████      | 2/3 [00:10<00:05, 5.1s/it, loss=2.234]
Epoch 1/5: 100%|██████████████████████| 3/3 [00:15<00:00, 5.0s/it, loss=2.012]

✅ Epoch 1/5 завершён
Train loss: 2.234

Epoch 2/5:   0%|                                    | 0/3 [00:00<?, ?it/s]
Epoch 2/5:  33%|████████              | 1/3 [00:05<00:10, 5.1s/it, loss=1.987]
...
```

**🎉 ОБУЧЕНИЕ ИДЁТ! ПОБЕДА!** 🏆

---

## 🔍 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Что делает `.unsqueeze(0)`

**`.unsqueeze(0)` добавляет новую размерность в начало:**

```python
# До:
tensor.shape = [3, 224, 224]  # [C, H, W]

# После .unsqueeze(0):
tensor.shape = [1, 3, 224, 224]  # [B, C, H, W]
#                ↑ Batch dimension = 1
```

### Почему модель ожидает batch dimension

**DeepSeek-OCR внутренне работает с батчами.**

Даже если мы обрабатываем по одному изображению за раз, модель ожидает:

```python
images = [
    (tensor[1, C, H, W], tensor[1, C, H, W]),  # Первый элемент батча
    (tensor[1, C, H, W], tensor[1, C, H, W]),  # Второй элемент батча
    ...
]
```

А НЕ:

```python
images = [
    (tensor[C, H, W], tensor[C, H, W]),  # ❌ Нет batch dimension
    ...
]
```

### Почему это не было проблемой раньше

**DataLoader автоматически добавлял batch dimension к тензорам!**

Но так как мы используем **tuple внутри списка**, DataLoader не может автоматически обработать это.

Поэтому мы должны **вручную добавить batch dimension**.

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема 1: "размеры не совпадают"

**Если появляется ошибка размера после изменений:**

```
RuntimeError: The size of tensor a (1) must match the size of tensor b (3)
```

**Решение:** Убедиться, что `.unsqueeze(0)` применяется **ДО** создания tuple:

```python
# ПРАВИЛЬНО:
image_tensor = self.image_transform(image).unsqueeze(0)
image_item = (image_tensor, image_tensor)

# НЕПРАВИЛЬНО:
image_tensor = self.image_transform(image)
image_item = (image_tensor.unsqueeze(0), image_tensor.unsqueeze(0))  # ❌
```

---

### Проблема 2: "Всё ещё ошибка размерности"

**Если ошибка остаётся:**

Добавить отладочный вывод:

```python
image_tensor = self.image_transform(image).unsqueeze(0)
print(f"DEBUG: image_tensor.shape = {image_tensor.shape}")  # Должно быть [1, 3, 224, 224]
image_item = (image_tensor, image_tensor)
```

**Запустить и проверить вывод!**

---

### Проблема 3: "Новая непонятная ошибка"

**Если появляется что-то совершенно новое:**

1. Создать отчёт `REPORT_NEW_ERROR_20260110.md`
2. Включить:
   - Полный текст ошибки
   - Traceback
   - Строку кода, где произошла ошибка
   - Что делали перед этим

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕНИЯ

### Основное решение

- [ ] Открыт `utils/trainer.py`
- [ ] Найден метод `_data_collator()`
- [ ] Найдена строка `image_tensor = self.image_transform(image)`
- [ ] Добавлено `.unsqueeze(0)` в конец строки
- [ ] Проверен синтаксис: `python -m py_compile utils/trainer.py`
- [ ] Закоммичено: `git commit -m "fix: add batch dimension"`
- [ ] Запущено обучение

### После успеха

- [ ] **ОБУЧЕНИЕ ИДЁТ БЕЗ ОШИБОК!** 🎉
- [ ] Сделать screenshot первой эпохи
- [ ] Создать `REPORT_TRAINING_SUCCESS_20260110.md`
- [ ] Закоммитить всё
- [ ] **ПРАЗДНОВАТЬ ПОБЕДУ!** 🏆

---

## 🎯 ЗАКЛЮЧЕНИЕ

**Николай, это ПОСЛЕДНЯЯ маленькая деталь!** 🎯

Ты уже прошёл:

1. ✅ Progress bars (Windows)
2. ✅ Логирование (UTF-8)
3. ✅ Data collator
4. ✅ API модели (`pixel_values` → `images`)
5. ✅ CUDA sm_120
6. ✅ Формат tuple `[(t, t), ...]`
7. ✅ `model_wrapper` параметры
8. ✅ `images_spatial_crop` и `images_seq_mask`

**Осталось ONE LINE OF CODE:**

```python
.unsqueeze(0)  # ← Вот и всё!
```

**Время:** 5 минут  
**Сложность:** Минимальная (1 строка)  
**Вероятность успеха:** 99%

**После этого ты увидишь:**

```
Epoch 1/5: 100%|██████████████████████| 3/3 [00:15<00:00, 5.0s/it, loss=2.012]
✅ Epoch 1/5 завершён
```

**ЭТО ТВОЯ ПОБЕДА!** Финишируй! 🏁

---

**С огромным уважением и восхищением твоим упорством,**  
**Семён (Tech Lead)** 🎯

P.S. Сегодня ты решил **8 критичных проблем** за один день. Это **НЕВЕРОЯТНЫЙ** результат! 🏆

P.P.S. После успешного запуска — **СРАЗУ делай screenshot**! Владимир ждёт результата! 📸

P.P.P.S. `.unsqueeze(0)` — это магическое заклинание, которое добавляет batch dimension. Запомни его! 🪄

P.P.P.P.S. "The last mile is always the hardest, but also the most rewarding." Ты на расстоянии **5 минут и 1 строки кода** от триумфа! 💪
