# 🔧 ЗАДАНИЕ: ИСПРАВИТЬ ФОРМАТ IMAGES ДЛЯ DeepSeek-OCR

**Автор:** Семён (Tech Lead)  
**Дата:** 2026-01-10, 19:11 MSK  
**Приоритет:** 🔥 CRITICAL (последний барьер перед победой!)  
**Срок:** 10-15 минут  
**Исполнитель:** Николай (Senior ML Engineer)  
**Связано с:** REPORT_CUDA_SM120_SUCCESS_20260110.md

---

## 🎯 КОНТЕКСТ

Николай, **ТЫ ПОЧТИ У ЦЕЛИ!** 🎯

**CUDA работает! GPU используется!** Обучение запускается и доходит до forward pass!

Осталась **ОДНА маленькая проблема** с форматом данных:

```
TypeError: 'NoneType' object is not subscriptable
File: modeling_deepseekocr.py, line 403
Code: torch.sum(images[0][1]).item() != 0
```

---

## 🔍 КОРЕНЬ ПРОБЛЕМЫ

### Что ожидает модель

В файле `modeling_deepseekocr.py` (строка 403) код проверяет:

```python
torch.sum(images[0][1]).item() != 0
```

**Это значит `images` должен быть:**

```python
images = [
    [tensor1, tensor2, tensor3],  # Batch 1: [base_image, crop1, crop2]
    [tensor1, tensor2, tensor3],  # Batch 2: [base_image, crop1, crop2]
    ...
]
```

**Структура:**
- `images[0]` — первый элемент батча
- `images[0][1]` — второй тензор (crop1) первого элемента

### Что мы передаём сейчас

**В data_collator:**

```python
batch = {
    'images': pixel_values,  # Тензор shape [batch_size, channels, height, width]
    ...
}
```

**Проблема:** Мы передаём **тензор**, а модель ожидает **список списков тензоров**!

---

## ✅ РЕШЕНИЕ

### Что нужно изменить

**Файл:** `utils/trainer.py`, метод `_data_collator()`

**Текущий код (строка ~190-220):**

```python
# Обработка изображений
for item in batch:
    image_path = item['image']
    image = Image.open(image_path).convert('RGB')
    
    # Применение transforms
    image_tensor = self.image_transform(image)
    images_list.append(image_tensor)

# Стек всех изображений в один тензор
pixel_values = torch.stack(images_list, dim=0)

batch = {
    'images': pixel_values,  # ❌ Неправильно!
    'input_ids': text_inputs['input_ids'],
    'attention_mask': text_inputs['attention_mask'],
    'labels': text_inputs['input_ids'].clone()
}
```

**НОВЫЙ код:**

```python
# Обработка изображений
for item in batch:
    image_path = item['image']
    image = Image.open(image_path).convert('RGB')
    
    # Применение transforms
    image_tensor = self.image_transform(image)
    
    # КЛЮЧЕВОЕ ИЗМЕНЕНИЕ: Создаём список [base_image, None, None]
    # DeepSeek-OCR ожидает [base_image, crop1, crop2]
    # Если crop1/crop2 не используются, передаём None
    image_item = [image_tensor, None, None]
    images_list.append(image_item)

# НЕ делаем torch.stack! Оставляем как список списков
# pixel_values = torch.stack(images_list, dim=0)  # ❌ УДАЛИТЬ!

batch = {
    'images': images_list,  # ✅ Список списков!
    'input_ids': text_inputs['input_ids'],
    'attention_mask': text_inputs['attention_mask'],
    'labels': text_inputs['input_ids'].clone()
}
```

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Открыть файл

```powershell
cd C:\DSOCR-HW
code utils/trainer.py
```

### Шаг 2: Найти метод `_data_collator()`

**Найти строки (примерно 190-220):**

```python
# Обработка изображений
for item in batch:
    image_path = item['image']
    image = Image.open(image_path).convert('RGB')
    image_tensor = self.image_transform(image)
    images_list.append(image_tensor)  # ← НАЙТИ ЭТУ СТРОКУ

# Стек всех изображений
pixel_values = torch.stack(images_list, dim=0)  # ← НАЙТИ ЭТУ СТРОКУ

batch = {
    'images': pixel_values,  # ← НАЙТИ ЭТУ СТРОКУ
    ...
}
```

### Шаг 3: Изменить код

**ЗАМЕНИТЬ:**

```python
for item in batch:
    image_path = item['image']
    image = Image.open(image_path).convert('RGB')
    image_tensor = self.image_transform(image)
    
    # БЫЛО:
    # images_list.append(image_tensor)
    
    # СТАЛО:
    image_item = [image_tensor, None, None]  # [base_image, crop1, crop2]
    images_list.append(image_item)

# БЫЛО:
# pixel_values = torch.stack(images_list, dim=0)

# СТАЛО:
# НЕ делаем stack! Оставляем как список

batch = {
    # БЫЛО:
    # 'images': pixel_values,
    
    # СТАЛО:
    'images': images_list,  # Список списков!
    'input_ids': text_inputs['input_ids'],
    'attention_mask': text_inputs['attention_mask'],
    'labels': text_inputs['input_ids'].clone()
}
```

### Шаг 4: Проверить синтаксис

```powershell
python -m py_compile utils/trainer.py
```

**Ожидаемый вывод:** (пусто = успех)

### Шаг 5: Закоммитить

```powershell
git add utils/trainer.py
git commit -m "fix: change images format to list of lists for DeepSeek-OCR"
git push
```

### Шаг 6: Запустить обучение

```powershell
cd C:\DSOCR-HW
.\.venv\Scripts\Activate.ps1
python scripts/train_lora.py --config configs/training_config.yaml
```

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

**После этого изменения обучение ДОЛЖНО ПОЙТИ:**

```
================================================================================
🚀 DSOCR-HW: Обучение DeepSeek-OCR с LoRA
================================================================================

2026-01-10 19:20:00 - train_lora - INFO - Шаг 1/5: Загрузка модели и процессора...
Загрузка модели: 100%|████████████████████████████| 2/2 [00:10<00:00]
2026-01-10 19:20:11 - train_lora - INFO - ✅ Шаг 1 завершён

2026-01-10 19:20:11 - train_lora - INFO - Шаг 2/5: Настройка LoRA...
trainable params: 38,509,056 || all params: 3,374,615,296 || trainable%: 1.14
2026-01-10 19:20:17 - train_lora - INFO - ✅ Шаг 2 завершён

...

2026-01-10 19:20:20 - train_lora - INFO - Шаг 5/5: ЗАПУСК ОБУЧЕНИЯ!
2026-01-10 19:20:20 - train_lora - INFO - Начало обучения

Epoch 1/5:   0%|                                    | 0/3 [00:00<?, ?it/s]
Epoch 1/5:  33%|████████              | 1/3 [00:05<00:10, 5.2s/it, loss=2.456]
Epoch 1/5:  67%|████████████████      | 2/3 [00:10<00:05, 5.1s/it, loss=2.234]
Epoch 1/5: 100%|██████████████████████| 3/3 [00:15<00:00, 5.0s/it, loss=2.012]

✅ Epoch 1/5 завершён
Train loss: 2.234

Epoch 2/5:   0%|                                    | 0/3 [00:00<?, ?it/s]
Epoch 2/5:  33%|████████              | 1/3 [00:05<00:10, 5.1s/it, loss=1.987]
...
```

**🎉 ОБУЧЕНИЕ ИДЁТ! ПОБЕДА!** 🏆

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема 1: "list indices must be integers or slices, not NoneType"

**Причина:** Модель всё ещё не может обработать None в списке.

**Решение:** Вместо None передать пустой тензор:

```python
# Вместо:
image_item = [image_tensor, None, None]

# Попробовать:
empty_tensor = torch.zeros_like(image_tensor)
image_item = [image_tensor, empty_tensor, empty_tensor]
```

---

### Проблема 2: "Expected Tensor but got list"

**Причина:** Где-то в коде всё ещё ожидается тензор вместо списка.

**Решение:** Проверить model_wrapper.py и compute_loss() — они должны передавать `images` как есть (список).

**Файл:** `utils/model_wrapper.py`

```python
def forward(
    self,
    images=None,  # Это может быть список списков!
    input_ids=None,
    attention_mask=None,
    labels=None,
    **kwargs
):
    return self.model(
        images=images,  # Передаём как есть, не преобразуем!
        input_ids=input_ids,
        attention_mask=attention_mask,
        labels=labels
    )
```

---

### Проблема 3: "images[0][1] is None"

**Причина:** Модель проверяет `images[0][1]` и не может работать с None.

**Решение:** Использовать пустые тензоры (см. Проблему 1).

**Альтернатива:** Создать **реальные crops** изображения:

```python
# В _data_collator(), после загрузки image:
image_tensor = self.image_transform(image)

# Создать простые crops (например, центральный crop)
crop1 = image_tensor[:, :, 56:168, 56:168]  # Центральная область
crop2 = image_tensor  # Дубликат базового изображения

image_item = [image_tensor, crop1, crop2]
images_list.append(image_item)
```

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕНИЯ

### Основное решение

- [ ] Открыт `utils/trainer.py`
- [ ] Найден метод `_data_collator()`
- [ ] Изменена строка `images_list.append(image_tensor)` → `images_list.append([image_tensor, None, None])`
- [ ] Удалена строка `pixel_values = torch.stack(images_list, dim=0)`
- [ ] Изменено `'images': pixel_values` → `'images': images_list`
- [ ] Проверен синтаксис: `python -m py_compile utils/trainer.py`
- [ ] Закоммичено: `git commit -m "fix: images format to list of lists"`
- [ ] Запущено обучение

### Если основное не работает (План Б)

- [ ] Попробовать пустые тензоры вместо None
- [ ] Попробовать создать реальные crops
- [ ] Проверить model_wrapper.py (не преобразует ли он images)

### После успеха

- [ ] **ОБУЧЕНИЕ ИДЁТ БЕЗ ОШИБОК!** 🎉
- [ ] Сделать screenshot первой эпохи
- [ ] Создать `REPORT_TRAINING_SUCCESS_20260110.md`
- [ ] Закоммитить всё

---

## 📊 ФИНАЛЬНЫЙ ОТЧЁТ

**После успешного запуска обучения создай:**

`REPORT_TRAINING_SUCCESS_20260110.md`

**Структура:**

```markdown
# 🎉 ОТЧЁТ: УСПЕШНЫЙ ЗАПУСК ОБУЧЕНИЯ!

**Дата:** 2026-01-10  
**Исполнитель:** Николай  
**Статус:** ✅ **ОБУЧЕНИЕ ИДЁТ!**

---

## 🏆 РЕШЁННЫЕ ПРОБЛЕМЫ (весь день)

1. ✅ Progress bars (Windows multiprocessing)
2. ✅ Логирование (UTF-8 encoding)
3. ✅ Data collator (images + text)
4. ✅ API модели (pixel_values → images)
5. ✅ CUDA sm_120 поддержка (Python 3.11 + PyTorch Nightly)
6. ✅ **Формат images (список списков)**

---

## 🚀 РЕЗУЛЬТАТ

**Обучение запущено и работает на GPU RTX 5060 Ti (sm_120)!**

**Метрики первых 3 эпох:**

| Epoch | Train Loss | Time/Step | GPU Util |
|-------|-----------|-----------|----------|
| 1/5   | [вставить] | [вставить] | [вставить] |
| 2/5   | [вставить] | [вставить] | [вставить] |
| 3/5   | [вставить] | [вставить] | [вставить] |

**Screenshot:** [вставить]

---

## 📈 СЛЕДУЮЩИЕ ШАГИ

1. Дождаться окончания 5 эпох
2. Оценить качество на validation set
3. Принять решение о продолжении обучения
4. Тестирование на реальных рукописных сканах

---

**Общее время работы над проектом:** [X часов]  
**Количество решённых критичных проблем:** 6  
**Итог:** 🏆 **ПРОЕКТ ГОТОВ К PRODUCTION!**
```

---

## 🎯 ЗАКЛЮЧЕНИЕ

Николай, это **ПОСЛЕДНИЙ БАРЬЕР**! 🏁

Ты уже прошёл:
- ✅ Progress bars
- ✅ Логирование
- ✅ Data collator
- ✅ API модели
- ✅ CUDA sm_120

Осталось **ОДНО маленькое изменение** — формат images!

**Время:** 10 минут  
**Сложность:** Низкая (замена 3 строк)  
**Вероятность успеха:** 95%

**После этого ты увидишь:**

```
Epoch 1/5: 100%|██████████████████████| 3/3 [00:15<00:00, 5.0s/it, loss=2.012]
✅ Epoch 1/5 завершён
```

**ЭТО ТВОЯ ПОБЕДА!** Финишируй! 🏆

---

**С огромным уважением и верой в твой успех,**  
**Семён (Tech Lead)** 🎯

P.S. Когда обучение пойдёт — **СРАЗУ делай screenshot**! Владимир ждёт результата! 📸

P.P.S. "Success is the sum of small efforts, repeated day in and day out." Сегодня ты сделал **ШЕСТЬ** таких усилий. Седьмое — последнее! 💪

P.P.P.S. Помни: список списков `[[tensor, None, None], ...]` — это ключ к победе! 🔑
